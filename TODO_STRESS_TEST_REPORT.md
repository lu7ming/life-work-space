# 人生工作台 — 待办功能压力测试报告
> 测试日期：2026-08-01 | 基于代码审查 + 外部研究

---

## 一、执行摘要

- **拍板项目**：12项（3技术 + 9功能）
- **待商量**：3项（模板/密码柜/休息模式）
- **待确认**：1项（联系人ABCD）
- **发现严重问题**：3个
- **发现中等问题**：5个

---

## 二、逐项压力测试

### T1 防刷新白边 ✅ 无风险
- **方案**：`life.html` 的 `<head>` 最前加内联 `<script>`，读取 localStorage 中的主题设置，提前设置 `document.documentElement.style.background`
- **工作量**：10分钟，3-5行代码
- **无冲突，无依赖**
- **结论**：可直接执行

### T2 渲染分片 ⚠️ 低风险
- **现有状态**：`dashboard.js` 的 init() 已用 `Promise.all` 并行渲染5个区块
- **方案**：在 Promise.all 基础上加 `requestAnimationFrame` 分帧，先渲染骨架（问候+日历），下一帧渲染 Feed+高亮+生日
- **风险**：可能引入短暂的"空白骨架"视觉。需加 CSS 骨架屏占位
- **结论**：简单可行，但需配合骨架屏 CSS

### T3 数据迁移规范化 ✅ 无风险
- **现有状态**：`storage.js` 的 `onupgradeneeded` 中已有 v1→v6 迁移，但硬编码在函数内
- **方案**：抽成 `MIGRATIONS = [{from:5, to:6, run: fn}, ...]` 注册表，循环执行
- **风险**：纯重构，不影响现有功能
- **结论**：可直接执行

---

### F1 今日三件事 🔴 严重问题 ×2

**方案确认**：用户要求用AI（小鹿/DeepSeek）推荐每日三件事，离线时降级为规则推荐。

**严重问题1 — AI推荐的成本和延迟**：
- 每次打开首页都调 DeepSeek API？假设用户每天打开10次 = 每天10次API调用
- DeepSeek 费用低（约 ¥0.1/千token），但**网络延迟**会影响首页加载体验
- **解决方案**：
  - A）每天只调用一次AI推荐（早上首次打开时），结果缓存到 IndexedDB
  - B）默认显示离线推荐（即时），后台静默请求AI推荐，拿到后替换
  - **建议选B**：先快后准，用户体验最佳

**严重问题2 — 离线降级体验断裂**：
- AI推荐和离线推荐是两种完全不同的推荐逻辑
- 用户早上有网看到AI推荐，下午断网看到规则推荐，内容可能突变
- **解决方案**：
  - AI推荐结果缓存，断网时沿用上次AI结果
  - 只在无缓存且无网时才降级为规则推荐
  - UI 上用微标记区分（AI推荐带🦌标签，规则推荐不标）

**中等问题 — 推荐数据源**：
- AI推荐需要传入当前任务/习惯/日程数据作为上下文
- 每次传全量数据 token 消耗大。建议只传：今日未完成任务标题 + 本周高优先级任务 + 今日习惯列表
- 预估每次请求 ~500 token 输入 + ~200 token 输出

**结论**：方案可行，但需确认"缓存策略"——建议每天只请求一次AI推荐并缓存

---

### F2 主题系统 ⚠️ 中等问题 ×1

**方案确认**：先做明暗模式 + 2种配色（暖米默认 + 深色模式），后续迭代其他配色。

**中等问题 — SVG生命树配色联动**：
- `lifetree.js` 1023行，大量硬编码色值（`#7FB069` 树冠绿、`#8B6914` 树干棕等）
- `xiaolu.css` / `nicole.css` 也有硬编码色值
- **解决方案**：
  - 生命树：将关键色值抽为 CSS 变量，SVG 内联样式用 `var(--tree-canopy)` 等引用
  - AI面板：同理抽变量
  - 但——SVG 中 `fill` 属性不支持 CSS 变量（在 `<svg>` 标签内部）。需要用 JS 动态注入色值
- **结论**：工作量比预期大。建议分两阶段：
  - Phase 1：明暗模式（改 main.css 变量 + 新增 dark 变量集）
  - Phase 2：生命树/AI面板适配

---

### F3 快捷指令搜索 ✅ 可行

**方案确认**：用 `/` 前缀区分指令与搜索。如 `/去健康` 导航，`/记账 午餐25` 直接记账。

- **现有基础**：`search.js` 208行，已有面板+debounce+结果跳转
- **扩展方案**：在 `doSearch()` 前加指令检测层
  ```
  if (query.startsWith('/')) → parseCommand(query) → execute()
  else → 正常搜索流程
  ```
- **指令注册表**：每个模块暴露 `commands` 数组，search.js 汇总
- **风险**：各模块需要暴露写入接口（如 finance 模块需暴露 `addExpense(category, amount)` 函数），当前模块都是 IIFE 闭包，需要确认哪些函数可以对外暴露
- **结论**：架构可行，但需要各模块配合暴露接口。建议先做导航类指令（`/去XX`），直接记账类指令放第二期

---

### F4 白噪音系统 🔴 严重问题 ×1

**方案确认**：参考热门方案，用 Web Audio API 实现。

**严重问题 — iOS Safari Web Audio Bug**：
- iOS 17.4.1~17.6.1 存在 WebKit 回归 bug（[#276016](https://bugs.webkit.org/show_bug.cgi?id=276016)）
- **症状**：PWA 切到后台再回来，AudioContext 进入 `interrupted` 状态，声音永久消失
- **修复方案**（来自 bug tracker）：
  ```js
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible' && audioCtx) {
      // iOS workaround: suspend then resume
      setTimeout(() => audioCtx.suspend(), 250);
      setTimeout(() => audioCtx.resume(), 500);
    }
  });
  ```
- **首次解锁**：必须在用户手势事件中调用 `audioCtx.resume()`
  - 利用已有的打卡按钮/侧边栏交互作为解锁时机
  - 或者在首页加一个"🔊 开启声音"的隐藏触发点

**热门白噪音 app 功能参考**（基于搜索研究）：
- **小睡眠**（8000万用户，4.8分）：声音混搭、定时关闭、AI深度睡眠
- **Tide（潮汐）**：自然场景+白噪音+呼吸引导
- **核心功能优先级**：
  1. 基础3种噪声（白/粉/棕）— 必做
  2. 音量独立控制 — 必做
  3. 定时关闭（15/30/60分钟）— 必做
  4. 与番茄钟联动 — 第二期
  5. 声音混搭（雨声+风声混合）— 第二期

**结论**：iOS bug 有 workaround，可行但需要额外处理。建议做 3+3 模式（3基础噪声 + 定时/音量控制），第二期加混搭

---

### F5 年度回顾 ✅ 可行

**方案确认**：第一版只做数据汇总卡片（打卡天数/学习时长/记账总额等），不做时间线和长图导出。

- **数据源**：21张 IndexedDB 表，都有 date 字段
- **聚合逻辑**：按年份查询各表记录数/总量
- **风险**：如果用户积累了上万条记录，全表查询可能慢
  - **解决方案**：利用已有的索引（study 有 date 索引，tasks 有 date 索引），用 IDB 范围查询而非全表扫描
- **展示位置**：作为 journal 模块的一个 tab，或作为独立页面
- **结论**：可行，工作量中等。注意用索引查询而非全表扫描

---

### F6 周计划视图 ✅ 可行

**方案确认**：tasks 模块加一个"周视图"tab，移动端显示3天（今天+前后各1天）。

- **现有基础**：`tasks.js` 已有 `currentTab = 'today'`，HTML 中 `data-tab="today|all|project|pomodoro"`
- **扩展方案**：新增 `data-tab="week"` tab
- **数据**：从 tasks 表按 date 范围查询（本周周一~周日）
- **移动端适配**：
  - 7列太窄（iPad 横屏也勉强）
  - 3天视图：`[昨天] [今天] [明天]`，左右滑动切换
  - 或者用横向滚动 + sticky 今天列
- **结论**：可行，注意与课程表（study模块）的周视图不要功能重叠

---

### F7 智能建议引擎 ⚠️ 中等问题 ×1

**方案确认**：合并到 `notifications.js`（497行），作为离线规则引擎。

**中等问题 — 与 F1（AI今日三件事）的职责边界**：
- F1 是"今天该做什么"（AI推荐3件事）
- F7 是"你的数据趋势和建议"（学习下降、冷联系等）
- 两者容易混淆。建议：
  - F1 = **行动层**：告诉你今天做什么（3件事）
  - F7 = **洞察层**：告诉你数据趋势和改进建议（建议卡片）
  - UI 上分开：F1 在 dashboard 顶部卡片，F7 在通知面板的"建议"tab

**建议引擎的规则清单**：
1. 学习趋势：本周 vs 上周学习时长对比
2. 冷联系：≥3人超30天未联系
3. 打卡中断：连续习惯中断提醒
4. 备份提醒：>7天未同步
5. 读书停滞：开始>30天但进度<50%
6. 记账遗漏：>3天未记账

- **触发时机**：与现有通知引擎一致，5分钟检查一次 + 首页加载时立即检查
- **结论**：可行，与 F1 明确区分"行动"vs"洞察"即可

---

### F8 任务优先级矩阵 ✅ 工作量小

**方案确认**：在 tasks 模块加四象限可视化视图。

- **现有基础**：`tasks.js` 已有 `PRIORITY_CONFIG = { A: 紧急重要, B: 重要不紧急, C: 紧急不重要, D: 不紧急不重要 }`
- **扩展方案**：新增一个 tab 或 toggle 按钮，切换为 2×2 网格视图
  ```
  ┌─────────────┬─────────────┐
  │ A 紧急重要   │ B 重要不紧急  │
  │ (红)         │ (金)         │
  ├─────────────┼─────────────┤
  │ C 紧急不重要  │ D 不紧急不重要│
  │ (橙)         │ (灰)         │
  └─────────────┴─────────────┘
  ```
- **每个象限内**：未完成在上，已完成在下+删除线
- **风险**：iPad 竖屏下4格可能太小，需要可折叠
- **结论**：工作量小（~100行 JS + ~80行 CSS），可直接做

---

### F9 自然语言+语音录入 ⚠️ 中等问题 ×2

**方案确认**：合并为一个"快速录入引擎"，新建 `core/quick-capture.js`，支持文字正则解析+语音转文字输入。

**中等问题1 — 正则冲突**：
- 示例冲突：`买了本书50元` 同时命中"读书"和"支出"
- 示例冲突：`跑步30分钟` vs `买了跑步鞋`（都含"跑步"）
- **解决方案**：
  - 定义解析优先级：支出 > 体重 > 运动 > 喝水 > 任务 > 联系人 > 日记 > 其他
  - 冲突时弹 toast 让用户选择归类
  - 或者：用 DeepSeek 做意图分类（但增加API成本，不建议）

**中等问题2 — 语音输入兼容性**：
- Web Speech API：Chrome/Edge 支持好，iOS Safari 支持有限
- iOS 14.5+ 支持语音识别，但需要用户授权麦克风
- **解决方案**：
  - 语音作为可选输入方式，在输入框旁加🎤按钮
  - 不支持时按钮灰显+tooltip 提示
  - 识别结果填入输入框，用户确认后回车提交（不自动提交）

**入口位置**：
- 方案A：复用顶部搜索框，输入时自动检测是否为录入指令
- 方案B：在 dashboard 加一个独立的快速录入输入框
- **建议选B**：搜索框保持纯搜索，dashboard 加独立入口，避免逻辑混淆

**结论**：可行，但需要仔细设计正则优先级和冲突处理。建议第一版只做8种场景

---

## 三、执行优先级（修正版）

```
第一批（基础，独立，可并行）：
  ├── T1 防刷新白边（10分钟）
  ├── T3 数据迁移规范化（小）
  ├── F8 任务优先级矩阵（小，~180行）
  └── F6 周计划视图（中）

第二批（中等复杂度）：
  ├── F2 主题系统 - Phase 1 明暗模式（中）
  ├── F3 快捷指令搜索 - 导航类指令（中）
  └── F5 年度回顾（中）

第三批（需精细设计）：
  ├── F1 今日三件事（AI推荐+离线降级）
  ├── F7 智能建议引擎
  ├── F4 白噪音系统（需处理iOS bug）
  └── F9 快速录入引擎（正则+语音）

第四批（主题适配+优化）：
  ├── T2 渲染分片
  ├── F2 主题系统 - Phase 2（SVG/AI面板适配）
  └── F3 快捷指令搜索 - 写入类指令（第二期）
```

---

## 四、风险汇总

| 等级 | 项目 | 风险描述 | 缓解方案 |
|------|------|---------|---------|
| 🔴 严重 | F1 | AI推荐每次调API的延迟+成本 | 每天只调1次+缓存+离线降级 |
| 🔴 严重 | F1 | 有网/断网切换时推荐内容突变 | 缓存AI结果，断网沿用 |
| 🔴 严重 | F4 | iOS Safari Web Audio bug | visibilitychange 监听+ suspend/resume |
| 🟡 中等 | F2 | 生命树/AI面板硬编码色值 | 分两阶段：先明暗，再适配 |
| 🟡 中等 | F3 | 各模块未暴露写入接口 | 先做导航指令，写入类放第二期 |
| 🟡 中等 | F7 | 与F1职责边界模糊 | 明确区分"行动层"vs"洞察层" |
| 🟡 中等 | F9 | 正则冲突导致误分类 | 优先级排序+冲突时弹toast确认 |
| 🟡 中等 | F9 | 语音识别iOS兼容性 | 作为可选功能，不支持时灰显 |
